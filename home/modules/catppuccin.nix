{ ... }: {
    catppuccin = {
        flavor = "frappe";
        accent = "lavender";

        cursors = {
            enable = true;
            accent = "mauve";
        };

        gtk = {
            enable = true;
            size = "compact";
        };

        spotify-player.enable = true;
    };
}